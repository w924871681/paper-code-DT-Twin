# v1.1.6 audit evidence

This directory stores the machine-readable repository, test, manuscript,
figure, terminology/privacy, and release-integrity results for v1.1.6.

`local_verification.json` is produced before tagging.
`release_verification.json` is produced after the GitHub Release is public.
