# Reproducibility

Use the processed CSV/JSON files in `results/` for result verification and figure redesign.
Full reruns require the release assets listed in `assets/model_assets.csv`.
Do not tune the six-architecture bank, the 10% margin, or the locked test pools.
