# v1.2.2 - Final repository consistency hotfix

This patch replaces the historical public-facing `WMSE` label in the Table 1
selection rule with the manuscript terminology `MSE`, rebuilds the manuscript
PDF, and synchronizes current release metadata.

No experimental value, split, seed, architecture, candidate bank, baseline,
deployment limit, optimizer, update budget, margin, result, or figure data
changed. Historical `WMSE` fields remain only in immutable provenance and
legacy schemas.
